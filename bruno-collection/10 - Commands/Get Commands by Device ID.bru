meta {
  name: Get Commands by Device ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/commands?deviceId={{deviceId}}
  auth: basic
}

auth:basic {
  username: {{email}}
  password: {{password}}
}

